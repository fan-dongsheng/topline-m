import Vue from 'vue'

import { Button, Icon, NavBar, CellGroup, Field, Toast, CountDown } from 'vant'

Vue.use(Button)
  .use(Icon)
  .use(NavBar)
  .use(CellGroup)
  .use(Field)
  .use(Toast)
  .use(CountDown)
